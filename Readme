🧩 Zadanie rekrutacyjne – ASP.NET Core MVC (.NET 9)
📘 Opis projektu

Zadania zostały wykonane w architekturze ASP.NET Core MVC (.NET 9) z wykorzystaniem Entity Framework Core.
Projekt został podzielony na warstwy, aby zachować przejrzystość, testowalność i łatwość utrzymania.

🔹 Architektura

Models – definicje encji (Employee, Team, Vacation, VacationPackage) oraz relacji między nimi zgodnie ze schematem z zadania.

Repository – odpowiedzialne za logikę zapytań i komunikację z kontekstem danych (AppDbContext).

Services – zawiera logikę biznesową, m.in.:

CountFreeDaysForEmployee() – obliczanie liczby pozostałych dni urlopu,

IfEmployeeCanRequestVacation() – sprawdzanie, czy pracownik może złożyć nowy wniosek urlopowy.

Controllers – warstwa prezentacji, w której dane z serwisu są udostępniane do widoku.

Middleware – globalna obsługa błędów (ErrorHandlingMiddleware), która przechwytuje wyjątki i zwraca spójne komunikaty JSON.

⚙️ Zadanie 6 – Optymalizacja zapytań

Użycie Select() – zamiast pobierać całe encje, aplikacja pobiera tylko potrzebne kolumny.
Dzięki temu zapytanie SQL jest prostsze i przetwarza mniej danych.

Filtrowanie po stronie SQL (Where()) – warunki są stosowane jeszcze przed ToList(),
dzięki czemu EF Core generuje zoptymalizowane zapytanie SQL.

Użycie Include() dla powiązań – pozwala uniknąć problemu N+1 query,
pobierając dane relacyjne (np. urlopy i pakiety urlopowe) w jednym zapytaniu.

🧪 Testy jednostkowe

Projekt zawiera testy NUnit, które weryfikują metodę:
-IfEmployeeCanRequestVacation

zapewnia powtarzalność wyników — aplikacja zawsze startuje z tym samym zestawem danych.

Projekt korzysta z Entity Framework Core z SQL Server (LocalDB) w podejściu Code First.
Dzięki temu baza danych generowana jest automatycznie na podstawie modeli i relacji w kodzie.

🔹 Uruchomienie bazy danych:

Upewnij się, że masz zainstalowany SQL Server Express lub LocalDB (domyślnie instalowany z Visual Studio).

W pliku appsettings.json znajduje się konfiguracja połączenia:

"ConnectionStrings": {
  "DefaultConnection": "Server=(localdb)\\MSSQLLocalDB;Database=RekrutacjaDb;Trusted_Connection=True;TrustServerCertificate=True;"
}


Wykonaj migracje i utwórz bazę danych:

dotnet ef migrations add Init
dotnet ef database update



🚀 Jak uruchomić projekt lokalnie
1️⃣ Sklonuj repozytorium
git clone https://github.com/invisible555/ZadanieRekrutacyjneNET.git
cd ZadanieRekrutacyjneNET
2️⃣ Przywróć zależności
dotnet restore
3️⃣ Zbuduj rozwiązanie
dotnet build
4️⃣ Uruchom aplikację
dotnet run --project Rekrutacja

🧱 Dodatkowe informacje
W SeedData znajdują się przykładowi pracownicy (m.in. Anna, która wykorzystała pełny limit urlopowy).

Globalna obsługa błędów zapewnia spójne komunikaty HTTP w przypadku wyjątków